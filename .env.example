# Скопируйте в .env.production.local и подставьте URL воркера после:
#   cd tts-worker && npx wrangler login && npx wrangler deploy
#
# VITE_TTS_PROXY_URL=https://leksa-tts.<your-subdomain>.workers.dev
